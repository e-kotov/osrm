% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm_contract.R
\name{osrm_contract}
\alias{osrm_contract}
\title{Contract OSRM Graph for Contraction Hierarchies (CH)}
\usage{
osrm_contract(
  input_osrm,
  threads = 8L,
  verbosity = c("INFO", "NONE", "ERROR", "WARNING", "DEBUG"),
  core = 1,
  segment_speed_file = NULL,
  turn_penalty_file = NULL,
  edge_weight_updates_over_factor = 0,
  parse_conditionals_from_now = 0,
  time_zone_file = NULL,
  echo_cmd = FALSE,
  echo = TRUE,
  spinner = TRUE
)
}
\arguments{
\item{input_osrm}{A string. Base path to the `.osrm` files (without extension).}

\item{threads}{An integer. Number of threads to use; default `8`.}

\item{verbosity}{A string. Log verbosity level passed to `-l/--verbosity`
(one of `"NONE","ERROR","WARNING","INFO","DEBUG"`); default `"INFO"`.}

\item{core}{A numeric. Percentage of the graph to contract [0..1]; default `1.0`.}

\item{segment_speed_file}{A string or `NULL`. Path to nodeA,nodeB,speed CSV; default `NULL`.}

\item{turn_penalty_file}{A string or `NULL`. Path to from_,to_,via_nodes,penalties CSV; default `NULL`.}

\item{edge_weight_updates_over_factor}{A numeric. Threshold for logging large weight updates; default `0`.}

\item{parse_conditionals_from_now}{A numeric. UTC timestamp for conditional restrictions; default `0`.}

\item{time_zone_file}{A string or `NULL`. GeoJSON file for time zone boundaries; default `NULL`.}

\item{echo_cmd}{A logical. Print each command before running; default `FALSE`.}

\item{echo}{A logical. Stream stdout/stderr; default `TRUE`.}

\item{spinner}{A logical. Show spinner instead of live logs; default `TRUE`.}
}
\value{
A list with elements:
\describe{
  \item{osrm_path}{The normalized path to the input `.osrm` base (invisibly). This can be passed over to [osrm_start_server()].}
  \item{logs}{The `processx::run` result object.}
}
}
\description{
Run the `osrm-contract` tool to contract an OSRM graph for the CH pipeline.
After running, a companion `<base>.osrm.hsgr` file must exist to confirm success.
}
