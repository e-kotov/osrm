% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm_start_server.R
\name{osrm_start_server}
\alias{osrm_start_server}
\title{Start an OSRM MLD/CH server with `osrm-routed`}
\usage{
osrm_start_server(
  osrm_path,
  version = FALSE,
  help = FALSE,
  verbosity = c("INFO", "ERROR", "WARNING", "NONE", "DEBUG"),
  trial = FALSE,
  ip = "0.0.0.0",
  port = 5001L,
  threads = 8L,
  shared_memory = FALSE,
  memory_file = NULL,
  mmap = FALSE,
  dataset_name = NULL,
  algorithm = NULL,
  max_viaroute_size = 500L,
  max_trip_size = 100L,
  max_table_size = 100L,
  max_matching_size = 100L,
  max_nearest_size = 100L,
  max_alternatives = 3L,
  max_matching_radius = -1L,
  echo_cmd = FALSE
)
}
\arguments{
\item{osrm_path}{Character(1). Path to the `.osrm.mldgr` or `.osrm.hsgr` file}

\item{version}{Logical; if `TRUE`, prints version and exits}

\item{help}{Logical; if `TRUE`, prints help and exits}

\item{verbosity}{Character; one of `"NONE","ERROR","WARNING","INFO","DEBUG"`}

\item{trial}{Logical or integer; if `TRUE` or >0, quits after initialization (default: `FALSE`)}

\item{ip}{Character; IP address to bind (default: `"0.0.0.0"`)}

\item{port}{Integer; TCP port to listen on (default: `5000`)}

\item{threads}{Integer; number of worker threads (default: `8`)}

\item{shared_memory}{Logical; load graph from shared memory (default: `FALSE`)}

\item{memory_file}{Character or NULL; DEPRECATED (behaves like `mmap`)}

\item{mmap}{Logical; memory-map data files (default: `FALSE`)}

\item{dataset_name}{Character or NULL; name of shared memory dataset}

\item{algorithm}{Character or NULL; one of `"CH","CoreCH","MLD"`. If `NULL` (default), auto-selected based on file extension}

\item{max_viaroute_size}{Integer (default: `500`)}

\item{max_trip_size}{Integer (default: `100`)}

\item{max_table_size}{Integer (default: `100`)}

\item{max_matching_size}{Integer (default: `100`)}

\item{max_nearest_size}{Integer (default: `100`)}

\item{max_alternatives}{Integer (default: `3`)}

\item{max_matching_radius}{Integer; use `-1` for unlimited (default: `-1`)}

\item{echo_cmd}{A logical. Print each command before running; default `FALSE`.}
}
\value{
A `processx::process` object for the running server
}
\description{
Launches an `osrm-routed` process pointing at a localized OSRM graph (either `.osrm.mldgr` for MLD or `.osrm.hsgr` for CH/CoreCH).
}
